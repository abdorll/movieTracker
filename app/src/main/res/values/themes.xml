<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MovieTracker" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MovieTracker.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splashIconBackground</item>
        <item name="postSplashScreenTheme">@style/Theme.MovieTracker</item>
    </style>

</resources>
